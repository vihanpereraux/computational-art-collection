let aspectRatio = 16/9;
let canvasWidth = 390;
let canvasHeight = canvasWidth*aspectRatio;

function setup() {
  createCanvas(canvasWidth, canvasHeight);
}

function draw() {
  fill(255, 0 , 0)
  rect(0, 0, 200, 200)
}
